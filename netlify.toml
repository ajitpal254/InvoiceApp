[build]
  command = "npm run build"
  publish = "dist"
  ignore = "server/"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "server/"
  SECRETS_SCAN_OMIT_KEYS = "PORT"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
